# CentralMonitoringHub - v1.0-alpha
# Building cmh-view
Run: `yarn && yarn build`

# Serve cmh-view temporarily
Run: `yarn && yarn start`

# Build and run cmh-backend
Run: `bash build.sh && ./cmh-backend`
